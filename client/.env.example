VITE_API_BASE_URL=http://localhost:5000/api
VITE_MAPTILER_API_KEY=
# Optional: only used as a fallback when VITE_API_BASE_URL is unset. Change
# this if your local backend runs on a different port than 5000.
VITE_API_PROXY_TARGET=http://localhost:5000
